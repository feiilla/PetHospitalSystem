# 全局配置
spring:
  profiles:
    active: test
  servlet:
    multipart:
      max-file-size: 100MB  #单次上传文件最大不超过100MB
      max-request-size: 300MB  #文件总上传大小不超过300MB
  mvc:
    hiddenmethod:
      filter:
        enabled: true

# 修改tomcat默认端口
server:
  port: 8084